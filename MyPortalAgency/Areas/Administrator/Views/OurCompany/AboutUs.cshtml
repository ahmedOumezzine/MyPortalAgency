@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "AboutUs";
}
@section scripts{
    <script>
            var  LinkAdmin = '@Url.Action("AboutUs","OurCompany",new { Area="Administrator"})';
                var Linkback = '/';

        $(document).ready(function () {
            $("#submit").click(function (event) {
                event.preventDefault();

                var form = document.forms["frmOurStory"];
                var length = form.length;
                var result = "{";
                var index;

                for (i = 0; i < length; i++) {
                    var e = form[i].name;
                    var key = form[i].id;
                    var value = form[i].value;
                    var fieldType = form[i].type;
                    //
                    // Hiddin
                    //
                    if (fieldType == "hidden") {
                        result = result + '"' + key + '" : ' + '"' + value + '",';
                    } //
                    // RADIO BUTTON
                    //
                    if (fieldType == "radio") {
                        for (x = 0; x < form.elements[e].length; x++) {
                            if (form.elements[e][x].checked) {
                                index = x;
                            }
                        }
                        result = result + '"' + key + '" : ' + '"' + value + '",';
                    }
                    //
                    // TEXT, TEXTAREA, and DROPDOWN
                    //
                    if ((fieldType == "text") || (fieldType == "password") || (fieldType == "textarea")) {
                        result = result + '"' + key + '" : ' + '"' + value + '",';
                    }
                    // CHECKBOX
                    //
                    if (fieldType == "checkbox") {

                        if (form.elements[e].checked == true) {
                            value = "true";
                        }
                        if (form.elements[e].checked == false) {
                            value = "false";
                        }
                        result = result + '"' + key + '" : ' + '"' + value + '",';
                    }
                }


                    result = result.substring(0, result.length - 1);
                    result += "}";
                    call_admin(LinkAPI + '/PageView/EditPage', result);
              
            });
        });
        </script>
 
    <script>
        var urledit = '@Url.Action("Edit", "OurCompany",new {Page= "AboutUs" })';
        var urldelete = '@Url.Action("Delete", "OurCompany", new { Page = "AboutUs" })';
        Models.push(
            new Model("AboutUs", "AboutUs", "renderAdmin_OurStory"),
            new Model("PillarsofOurSuccess", "PillarsofOurSuccess", "renderAdmin_OurStory"),
            new Model("OurStory", "OurStory", "renderAdmin_OurStory")
        );
        calladmin(LinkAPI + '/PageView/GetPages/', 'GET', { type: 'AboutUs' }, Models);
    </script>
}


<ul class="simple-bullet-list mb-3">
    <li class="orange">
        <span class="title">General</span>
    </li>
</ul>

<div class="row">
    <div class="col-lg-12">
        <form id="frmOurStory" class="form-horizontal form-bordered">
            <section class="card">
                <div class="card-body">
                    <input type="hidden" id="Id" name="Id" class="form-control">

                    <div class="form-group row">
                        <label class="col-sm-4 control-label text-sm-left  pt-2">Your Title:</label>
                        <div class="col-sm-8">
                            <input type="text" id="Title" name="Title" class="form-control">
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-sm-4 control-label text-sm-left  pt-2">Your Description:</label>
                        <div class="col-sm-8">
                            <textarea id="Description" name="Description" class="form-control"></textarea>
                        </div>
                    </div>
                </div>
                <footer class="card-footer text-right">
                    <button id="submit" type="submit" class="btn btn-primary">Submit</button>
                </footer>
            </section>
        </form>
    </div>
</div>

<hr />

<ul class="simple-bullet-list mb-3">
    <li class="red">
        <span class="title">  ABOUT MyPortal</span>
    </li>
</ul>
<h4 class="pt-3 mb-0"></h4>

<div class="card-body" id="AboutUs">
    Content.
</div>


<hr />


<ul class="simple-bullet-list mb-3">

    <li class="green">
        <span class="title"> Our Story</span>
        <span class="description truncate text-right"><a asp-controller="OurCompany" asp-action="Create" asp-route-id="03F17127-5270-4067-9F5E-0C4B798BE221" asp-route-Type="OurStory" asp-route-Page="AboutUs" class="mb-1 mt-1 mr-1 btn btn-default">Add</a></span>
    </li>
</ul>


<div class="row" id="OurStory"></div>



<hr />


<ul class="simple-bullet-list mb-3">

    <li class="blue">
        <span class="title"> Pillars of Our Success</span>
        <span class="description truncate text-right"><a asp-controller="OurCompany" asp-action="Create" asp-route-id="03F17127-5270-4067-9F5E-0C4B798BE221" asp-route-Type="PillarsofOurSuccess" asp-route-Page="AboutUs" class="mb-1 mt-1 mr-1 btn btn-default">Add</a></span>
    </li>

</ul>

<div id="PillarsofOurSuccess" class="row"></div>


